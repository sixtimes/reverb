extends layout

block content
    if listings
        ul(class='list-group')
        each listing in listings.listings
            li(class='d-flex list-group-item flex-row align-items-center')
                div(class='p-2')
                    img(src=listing.photos[0]._links.thumbnail.href)
                h2(class='p-2')= listing.title